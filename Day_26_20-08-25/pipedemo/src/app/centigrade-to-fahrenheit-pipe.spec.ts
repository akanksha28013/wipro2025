import { CentigradeToFahrenheitPipe } from './centigrade-to-fahrenheit-pipe';

describe('CentigradeToFahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new CentigradeToFahrenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
